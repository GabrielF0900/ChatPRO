# Dockerfile para PostgreSQL simples
FROM postgres:15

# Variáveis de ambiente padrão (pode alterar ou usar .env no docker-compose)
ENV POSTGRES_USER=postgres
ENV POSTGRES_PASSWORD=senha123
ENV POSTGRES_DB=chatpro

# Expõe a porta padrão do PostgreSQL
EXPOSE 5432
